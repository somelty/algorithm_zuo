第一个要注意的是，不能单纯根据函数的结构去判断它的时间复杂度。比如看到两个 for 循环就判断它是 O(n²) ，这是一种非常低级的错误。

这里有三个例子：

1. 冒泡排序（用 while 实现）
   虽然它只有一个 while 循环，但它的时间复杂度实际上是一个 Big O(n²) 的复杂度。

2. 两个 for 循环嵌套（步长为 1）
   外层 for 循环的 i 从 1 到 n，步长为 1；内层循环的 j 也是从 i 或 i+1 开始，步长同样为 1。这种情况下，它的时间复杂度确实是 O(n²)。

3. 两个 for 循环嵌套（步长动态变化）
   如果第二层 for 循环的步长 j 不是 1，而是 i（即每次跳 i 步），那么时间复杂度就是 O(n log n)。

    之所以是 O(n log n)，是因为这里涉及到一个调和级数：
    1 + 1/2 + 1/3 + 1/4 + ... + 1/n
    这个级数是收敛于 log n 的。

    具体分析这个流程：
    (a) 当 i = 1 时，第二层嵌套的 j 跑了 n 步
    (b) 当 i = 2 时，每隔两个跳一次，所以跳的次数是 n/2
    (c) 当 i = 3 时，跳的次数是 n/3
    (d) 以此类推，直到 n/n

    相当于在调和级数的整体基础上乘以一个 n，所以它的时间复杂度就是 n × log n。

所以说，判断时间复杂度主要还是要对业务逻辑结构和运行逻辑非常熟悉，而不是只看函数的表面结构。

第二个要注意的地方是：

1. 对于固定流程的函数，我们估计它的时间复杂度时，用的是最坏情况。
2. 对于非固定流程（比如随机插入），因为它的核心流程是随机的，所以要用它的平均情况（或者叫期望值）去估计。
我们这个地方的理解是：比如冒泡排序和选择排序，它们的最好、最坏和平均时间复杂度都是一样的。但插入排序不一样，它的最好情况时间复杂度是 Big O(n)，而最坏情况是 Big O(n²)，所以综合来看它的时间复杂度是 Big O(n²)。

为什么呢？

1. 最好情况：
   如果我们要做从小到大排序，而数组原本就是一个从小到大排序好的状态。此时第一层 for 循环 i 从 1 位置开始遍历到 n 位置（即最后一个位置），它实际上只需遍历一遍数组，不需要做任何再往前遍历的交换操作，所以时间复杂度是 n。

2. 最坏情况（逆序）：
   比如数组是 5, 4, 3, 2, 1 这种逆序状态。
   (a) i 从第二个位置（即数字 4）开始一直到最后一个位置。
   (b) 对于每一个 i，内部的 j 从 i - 1 开始一直往前遍历到 0。
   (c) 举个例子，当 i 在 1 位置（数字 4）时，j 在 0 位置（数字 5）。比较 j 和 j + 1，发现 5 比 4 大，于是交换位置。
   (d) 当 i 到达最后一个位置时，j 相当于要从 i - 1 开始不断比较并交换，同时只要发生了交换，就要继续往前走，依次直到数组开头。

在这种情况下，当 i 在最后一个位置时，j 需要处理的数据量是 n 级别的（相当于数组长度）。整体计算下来，总的交换次数其实是 1 + 2 + 3 + 4 + ... + (n - 1)。

这是一个等差数列求和，它可以写成一个二次方程的形式（相当于 An² + Bn + C）。舍去常数项和一阶项后，它就是一个 O(n²) 时间复杂度级别的算法

